services:
  renderer.bluemoonformsdev.com:
    container_name: renderer.bluemoonformsdev.com
    build: .
    volumes:
      - ./:/usr/share/nginx/html
    networks:
      - default
networks:
  default:
    external: true
    name: dev-network
